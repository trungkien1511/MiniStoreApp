<Window x:Class="MiniStore.Views.User.MainLayout"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:user="clr-namespace:MiniStore.Views.User"
        Title="MainLayout" Height="900" Width="1200">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- HeaderBar -->
            <RowDefinition Height="*"/>
            <!-- Content -->
        </Grid.RowDefinitions>

        <!-- HeaderBar -->
        <user:HeaderBar Grid.Row="0"/>

        <!-- Phần nội dung -->
        <Grid Grid.Row="1" Background="#f1f1f1">
            <ContentControl  x:Name="MainContent" Margin="200 10 200 0" />
        </Grid>
    </Grid>
</Window>
